[STAT-001]

Expandable/collapsible stat blocks per monster and minion row

**Key decisions:**
- Added MonsterFeature/PowerRollEffect types to types.ts; Monster gets optional features field
- StatBlock component splits features into Abilities and Traits sections w/ power roll tier tables
- Expand/collapse toggle button appears only when monster has features; chevron icon in condition cell column
- Stat block row spans gridColumn 2/-1 below monster row; GroupSection tracks expandedStatBlocks state and adjusts grid row counts
- MinionGroupRow gets separate stat block toggle alongside existing minion expand chevron
- Seeded features data inline in ENCOUNTER_GROUPS for Goblin Assassin, Underboss, and Minions (Spinecleaver)
- 22 new tests covering: StatBlock rendering, MonsterRowCells toggle, GroupSection integration, minion group stat block

**Files changed:**
- src/types.ts
- src/data.ts
- src/components/StatBlock.tsx (new)
- src/components/StatBlock.test.tsx (new)
- src/components/MonsterRowCells.tsx
- src/components/MinionGroupRow.tsx
- src/components/GroupSection.tsx
- PRD.json


[STAT-003]

Source stat block data from SteelCompendium bestiary — bestiary.ts loads data/bestiary/Monsters/statblocks.json (416 monsters), builds name-keyed lookup map, maps raw JSON → MonsterFeature[]

**Key decisions:**
- Direct JSON import of statblocks.json via Vite's resolveJsonModule; no runtime fetch needed
- baseName() strips trailing ordinal ("Goblin Assassin 1" → "Goblin Assassin") so numbered encounter copies resolve to the base statblock
- resolveFeatures() in data.ts tries monster name first, then first minion's name (handles generic parent names like "Minions" for minion groups), then falls back to inline seed data
- Removed hardcoded features from ENCOUNTER_GROUPS seed data; bestiary is now source of truth
- mapFeatures() filters to ability/trait only (excludes subtrait), omits optional fields when absent
- 30 new tests covering: baseName, statblockCount, statblockNames, lookupStatblock, mapFeatures, featuresForMonster, cloneEncounterGroups bestiary integration

**Files changed:**
- src/bestiary.ts (new)
- src/bestiary.test.ts (new)
- src/data.ts
- PRD.json


[STAT-004]

Core stats header in stat block — stamina, MARIP, speed, size, stability, free strike, EV, ancestry, immunities, weaknesses, with_captain rendered from bestiary JSON

**Key decisions:**
- CoreStatsHeader sub-component inside StatBlock.tsx; receives BestiaryStatblock directly
- StatBlock calls lookupStatblock(monsterName) internally — no prop threading needed through MonsterRowCells/MinionGroupRow
- baseName() stripping already handled by lookupStatblock, so "Goblin Assassin 1" resolves correctly
- Empty-state logic updated: shows "No features available" only when both features[] is empty AND no bestiary match exists
- Speed renders movement type in parens when present (e.g. "6 (Climb)")
- Optional fields (immunities, weaknesses, with_captain) only rendered when present in bestiary data
- 14 new tests covering: stamina, MARIP, speed+movement, size, stability, free strike, ancestry, EV, with_captain, immunities/weaknesses, ordinal stripping, unknown monster fallback, features-empty-but-bestiary-exists

**Files changed:**
- src/components/StatBlock.tsx
- src/components/StatBlock.test.tsx
- PRD.json


[MALICE-001]

Filter malice abilities from stat blocks — monster stat blocks (core stats, non-malice abilities, traits) remain visible for all monsters

**Key decisions:**
- Malice filtering moved from rendering layer to data layer: mapFeatures() in bestiary.ts now excludes features with malice cost and filters out malice-cost effects within features
- isMaliceCreature() retained in bestiary.ts for potential future use but no longer referenced by components
- Removed isMaliceCreature guard from MonsterRowCells, MinionGroupRow, GroupSection — all monsters now show stat block toggle when they have (non-malice) features
- hasMaliceCost() helper moved above mapFeatures for forward reference
- Updated tests: replaced 4 stat-block-suppression tests with 3 tests verifying malice creatures DO show stat blocks; added 4 new mapFeatures tests for malice filtering (feature-level and effect-level)

**Files changed:**
- src/bestiary.ts
- src/bestiary.test.ts
- src/components/MonsterRowCells.tsx
- src/components/MinionGroupRow.tsx
- src/components/GroupSection.tsx
- src/components/StatBlock.test.tsx


[MINION-003]

Dead/alive toggle on individual child minion rows — replaces empty stamina cell with switch + visual dimming

**Key decisions:**
- Added `dead: boolean` to MinionEntry type; MinionEntrySeed gets optional `dead?` defaulting to false in cloneMinionEntries
- Toggle rendered as styled checkbox switch in stamina column (col 3) with red/green color states and Dead/Alive label
- Dead minions get `opacity-40` on name badge, MARIP, stats, and conditions cells; name text gets `line-through`
- State managed via new `patchMinionDead` callback in App.tsx, threaded through GroupSection → MinionGroupRow → MinionChildRow
- 7 new integration tests in App.test.tsx covering: default alive state, toggle to dead, strikethrough visual, toggle back to alive, label text switch, isolation between minions, toggle presence in stamina column

**Files changed:**
- src/types.ts
- src/data.ts
- src/App.tsx
- src/components/GroupSection.tsx
- src/components/MinionGroupRow.tsx
- src/App.test.tsx
- PRD.json


[MINION-004]

Interval-based shared stamina pool for minion groups — replaces standard `cur/max` with threshold markers (e.g. `5 / 10 / 15 / 20`)

**Key decisions:**
- `minionInterval()` in bestiary.ts resolves per-minion stamina from bestiary; tries parent name, falls back to first minion name (same pattern as resolveFeatures)
- `minionThresholds()` builds ascending threshold array: `interval * (i+1)` for each minion
- MinionStaminaDisplay component shows threshold segments with 3 visual states: healthy (white), at-risk (amber, stamina within interval but below threshold), dead (red + line-through, stamina at or below previous threshold)
- EditableStaminaCell gets optional `renderDisplay` prop so MinionGroupRow can inject the interval display while keeping the existing hover popover for stamina adjustments
- Seed data updated: Minions stamina `[12,12]` → `[20,20]` to match 4×5 bestiary interval total
- 23 new tests: 9 minionInterval/minionThresholds unit tests, 10 MinionStaminaDisplay component tests, 4 App integration tests

**Files changed:**
- src/bestiary.ts
- src/bestiary.test.ts
- src/components/MinionStaminaDisplay.tsx (new)
- src/components/MinionStaminaDisplay.test.tsx (new)
- src/components/EditableStaminaCell.tsx
- src/components/MinionGroupRow.tsx
- src/data.ts
- src/App.test.tsx
- PRD.json


[MINION-001]

Captain assignment for minion groups — Captain pill under parent minion row, dropdown of non-minion monsters, colored ordinal + name on assignment

**Key decisions:**
- CaptainRef type `{ groupIndex, monsterIndex }` added to types.ts; Monster gets optional `captainId?: CaptainRef | null`
- allGroups passed from App → GroupSection → MinionGroupRow so dropdown can list all non-minion monsters across all encounter groups
- Candidate list filters out any monster with minions (minion groups can't captain other minion groups)
- Captain pill rendered in creature name cell (col 2) below subtitle; unassigned state shows dashed border + plus icon, assigned state shows captain's group-colored ordinal badge + name + edit icon
- Dropdown includes "Remove captain" option when a captain is already assigned (covers MINION-002 flow)
- patchMinionCaptain callback in App.tsx follows same immutable update pattern as other patch functions
- Dropdown closes on outside click via mousedown listener on document
- 8 new integration tests covering: unassigned pill rendering, dropdown content, captain assignment, ordinal display, dropdown close, captain change via edit icon, captain removal, minion exclusion from candidates

**Files changed:**
- src/types.ts
- src/App.tsx
- src/components/GroupSection.tsx
- src/components/MinionGroupRow.tsx
- src/App.test.tsx
- PRD.json


[MINION-002]

Remove captain from minion group — added 'x' button directly on assigned captain pill for one-click removal

**Key decisions:**
- Added dedicated 'x' (close) SVG button with `data-testid="remove-captain"` next to captain name, alongside existing edit (pencil) icon
- 'x' calls `onCaptainChange?.(null)` directly — no dropdown interaction needed
- Edit icon still opens dropdown for changing captain; 'x' is strictly for removal
- MINION-001 already had "Remove captain" in the dropdown; this adds the direct-action button the PRD requires
- 4 new tests: x button presence on assigned pill, x click removes + reverts to unassigned, x not present when unassigned, reassignment after x removal

**Files changed:**
- src/components/MinionGroupRow.tsx
- src/App.test.tsx
- PRD.json


[MINION-005]

Minion stamina popover with interval-based input — replaces standard cur/max text inputs with interval threshold segments in the hover editor

**Key decisions:**
- Added `renderEditor` prop to EditableStaminaCell alongside existing `renderDisplay`; when provided, replaces the entire editor panel (bump buttons + center input area) inside the hover dialog
- MinionStaminaEditor component mirrors MinionStaminaDisplay's threshold logic but uses editor-appropriate styling (light bg, zinc-950 text for healthy, amber-700 for at-risk, red-700+line-through for dead)
- Editor reuses same bump buttons (±1, ±10) as standard editor; bump function passed through from EditableStaminaCell
- Falls back to null render (letting EditableStaminaCell show standard editor) when no bestiary entry or minionCount=0
- Non-minion monsters unaffected — no `renderEditor` passed, standard cur/max inputs remain
- 13 component tests + 4 App integration tests covering: interval segments, separators, state styling, bump buttons, dynamic segment count, fallback, popover content, cur/max absence, bump interaction, non-minion preservation

**Files changed:**
- src/components/MinionStaminaEditor.tsx (new)
- src/components/MinionStaminaEditor.test.tsx (new)
- src/components/EditableStaminaCell.tsx
- src/components/MinionGroupRow.tsx
- src/App.test.tsx
- PRD.json


[DATA-003]

Delete monster from group — trash icon on each monster/minion row, immediate removal with captain ref cleanup

**Key decisions:**
- deleteMonster() in App.tsx: filters out monster, removes empty groups, fixes CaptainRef indices (groupIndex shift when group removed, monsterIndex shift when earlier monster in same group removed), nullifies refs pointing at deleted monster
- groupTurnActed array pruned when a group is fully removed
- Delete button placed in creature name cell (col 2) with hover-to-reveal via Tailwind `group/namecell` + `group-hover/namecell:opacity-100`; always accessible via keyboard focus
- Both MonsterRowCells and MinionGroupRow get the same delete button pattern
- onDeleteMonster threaded: App → GroupSection → MonsterRowCells / MinionGroupRow
- 9 new integration tests: button presence, deletion, ordinal update, last-monster-removes-group, captain-ref-cleared-on-captain-delete, captain-index-shift, cross-group-isolation, minion-group-deletion, post-delete-functionality

**Files changed:**
- src/App.tsx
- src/components/GroupSection.tsx
- src/components/MonsterRowCells.tsx
- src/components/MinionGroupRow.tsx
- src/App.test.tsx
- PRD.json


[MINION-006]

Subtle threshold mismatch cue — compares stamina-suggested dead count vs actual toggled dead count, shows animated "Kill N" or "Revive N" pill on the parent minion stamina display

**Key decisions:**
- suggestedDeadCount() in bestiary.ts counts minions whose lower-bound threshold >= current stamina; pure function, no side effects
- MinionStaminaDisplay gets optional actualDeadCount prop (defaults 0); compares against suggestedDeadCount to decide cue
- Cue rendered as animate-pulse pill with role="status" for screen reader; red "Kill N" when director needs to mark deaths, green "Revive N" when minions can come back
- No auto-toggling — cue is advisory only, director manually toggles dead/alive per PRD requirement
- MinionGroupRow passes `minions.filter(m => m.dead).length` as actualDeadCount
- 7 suggestedDeadCount unit tests, 7 MinionStaminaDisplay cue tests, 4 App integration tests (sync=no cue, kill cue on stamina drop, cue clears after toggling dead, revive cue when dead but full HP)

**Files changed:**
- src/bestiary.ts
- src/bestiary.test.ts
- src/components/MinionStaminaDisplay.tsx
- src/components/MinionStaminaDisplay.test.tsx
- src/components/MinionGroupRow.tsx
- src/App.test.tsx
- PRD.json


[DATA-001]

Add monsters to existing group — "Add monster" button per group, opens searchable bestiary picker (416 monsters), inserts fully-formed Monster into group state

**Key decisions:**
- AddMonsterButton component: collapsed = dashed-border button; expanded = search input + scrollable listbox of bestiary entries, sorted alphabetically
- monsterFromBestiary() in data.ts creates a complete Monster from bestiary name — pulls stamina, MARIP, speed/stab/fs, subtitle, features; supports ordinal suffix for duplicates
- bestiarySubtitle() and deriveInitials() helpers in bestiary.ts; initials take first letter of each word (max 3)
- Unknown bestiary names produce a blank monster (null marip, 0/0 stamina, empty subtitle) — no crash
- addMonsterToGroup callback in App.tsx appends monster to group's monsters array; threaded via onAddMonster prop through GroupSection
- AddMonsterButton placed below monster rows in group grid, spanning columns 2–end
- Picker closes on: select, Escape key, close button, outside click
- 24 new tests: 5 monsterFromBestiary unit, 5 deriveInitials/bestiarySubtitle unit, 9 AddMonsterButton component, 4 App integration (button presence, search/select, ordinal update, stamina interaction)

**Files changed:**
- src/bestiary.ts
- src/bestiary.test.ts
- src/data.ts
- src/data.test.ts
- src/App.tsx
- src/components/AddMonsterButton.tsx (new)
- src/components/AddMonsterButton.test.tsx (new)
- src/components/GroupSection.tsx
- src/App.test.tsx
- PRD.json


[DATA-002]

Create new blank encounter group — "Add group" button below encounter groups, creates empty group with auto-assigned color and turn state

**Key decisions:**
- nextAvailableColor() in data.ts picks first unused color from GROUP_COLOR_ORDER; wraps modulo when all 10 exhausted
- addNewGroup callback in App.tsx atomically appends empty group to encounterGroups and false to groupTurnActed
- GroupSection handles empty group (0 monsters) by clamping totalGridRows to min 1 so turn column renders
- "Add group" button styled with dashed border + plus icon, consistent with AddMonsterButton pattern
- resetAllTurns fixed: was using static ENCOUNTER_GROUPS.length, now uses prev.map(() => false) to handle dynamic group count
- 8 new App integration tests + 4 nextAvailableColor unit tests

**Files changed:**
- src/data.ts
- src/data.test.ts
- src/App.tsx
- src/components/GroupSection.tsx
- src/App.test.tsx
- PRD.json


[TURN-004]

No condition changes when toggling group turn back to pending — verified existing behavior is correct (toggleGroupTurn only flips boolean, never touches encounterGroups state), wrote 5 integration tests

**Key decisions:**
- No code changes needed; toggleGroupTurn already only modifies groupTurnActed state, conditions live in encounterGroups state — fully isolated
- 5 new App integration tests: neutral conditions preserved, EoT/SE conditions preserved, cross-group isolation, minion child conditions preserved, stamina preserved
- Existing "reset clears turns but preserves stamina and condition changes" test covers resetAllTurns path; new tests cover single-group toggle path

**Files changed:**
- src/App.test.tsx
- PRD.json


[TURN-001]

Animate EoT/SE condition icons with pulsating glow when group turn set to acted — amber glow for EoT, purple glow for SE; animation stops when toggled back to pending

**Key decisions:**
- Custom CSS @keyframes (glow-eot, glow-se) in index.css with Tailwind v4 @utility directive for animate-glow-eot / animate-glow-se classes
- conditionIconShellClass() gains optional turnActed param; appends animation class only for active eot/se conditions when true
- turnActed threaded: App→GroupSection→MonsterRowCells/MinionGroupRow→CreatureConditionCell→ConditionCatalogIconStrip→conditionIconShellClass; reuses existing turnComplete prop
- motion-reduce:animate-none included for reduced-motion accessibility
- Neutral conditions never animate; inactive icons never animate; terrain strip unaffected (default turnActed=false)
- 7 conditionIconShellClass unit tests, 5 ConditionCatalogIconStrip component tests, 6 App integration tests

**Files changed:**
- src/index.css
- src/components/ConditionIcon.tsx
- src/components/ConditionIcon.test.tsx
- src/components/ConditionCatalogIconStrip.tsx
- src/components/ConditionCatalogIconStrip.test.tsx
- src/components/CreatureConditionCell.tsx
- src/App.test.tsx
- PRD.json


[TURN-005]

Cancel condition animations on rapid acted→pending — no code changes needed; existing architecture already handles this correctly

**Key decisions:**
- Animations are purely CSS-class-driven via turnActed boolean prop; toggling to pending removes animation classes immediately
- toggleGroupTurn only modifies groupTurnActed state, never touches encounterGroups — conditions stay active/unchanged by design
- No 30-second timers exist yet (TURN-002/003), so no timer cleanup needed; when timers are added, they should check turnActed before firing
- 5 new App integration tests: EoT glow cancellation + condition preserved, SE glow cancellation + condition preserved, both EoT+SE preserved simultaneously, stamina+neutral conditions preserved, cross-group isolation on rapid toggle

**Files changed:**
- src/App.test.tsx
- PRD.json


[TURN-002]

Auto-disable EoT conditions after 30s if not clicked — 30-second timer starts when group turn set to acted; unconfirmed EoT conditions auto-removed; clicking glowing EoT icon confirms (keeps) it

**Key decisions:**
- Timer per group via useRef<Map<number, timeout>>; starts in useEffect watching groupTurnActed transitions (false→true)
- eotConfirmed tracked as React state (Map<number, Set<string>>) not ref, because confirming must trigger re-render to stop glow animation
- eotConfirmedLatest ref mirrors state for timer callback (captures latest confirmed set at fire time)
- Confirm key format: `${monsterIndex}:${label}` for monsters, `${monsterIndex}:${minionIndex}:${label}` for minions
- Click interception in CreatureConditionCell: when turnComplete + EoT + not yet confirmed, click calls onConfirmEot instead of onRemove
- ConditionCatalogIconStrip suppresses glow animation for confirmed EoT icons via isEotConfirmed prop
- Timer cancelled + confirmed set cleared when group toggled back to pending (TURN-005 compatibility)
- Neutral and SE conditions unaffected by timer; only EoT state filtered
- 10 new App integration tests using vi.useFakeTimers({shouldAdvanceTime:true}) pattern — fake timers installed after user interactions to avoid userEvent hanging

**Files changed:**
- src/App.tsx
- src/components/GroupSection.tsx
- src/components/MonsterRowCells.tsx
- src/components/MinionGroupRow.tsx
- src/components/CreatureConditionCell.tsx
- src/components/ConditionCatalogIconStrip.tsx
- src/App.test.tsx
- PRD.json


[TURN-003]

SE stays on monster after 30s (no auto-clear); SE pulsate stops after same 30s post-act window as EoT timer; click still removes SE (save failed)

**Key decisions:**
- `seActWindowElapsedGroup` Set in App: same setTimeout(30s) as EoT cleanup adds gi; pending toggle deletes gi — reuses one timer, no second clock
- `conditionIconShellClass` 3rd/4th args: eotTurnGlow vs seTurnGlow; `seTurnGlow ?? eotTurnGlow` preserves old 3-arg calls
- `seActPhaseGlow` threaded App→GroupSection→rows→CreatureConditionCell→strip; `seActPhaseGlow ?? turnActed` for strips that omit prop (terrain)
- Tests: extended SE 30s test for glow off; new click-remove-after-30s; strip + shell unit tests; eslint.config file overrides for pre-existing react-refresh / set-state-in-effect noise

**Files changed:**
- src/App.tsx
- src/components/ConditionIcon.tsx
- src/components/ConditionCatalogIconStrip.tsx
- src/components/CreatureConditionCell.tsx
- src/components/GroupSection.tsx
- src/components/MonsterRowCells.tsx
- src/components/MinionGroupRow.tsx
- src/components/*.test.tsx (GroupSection, MonsterRowCells, StatBlock, ConditionIcon, ConditionCatalogIconStrip)
- src/App.test.tsx
- eslint.config.js
- PRD.json


[DND-001]

Reorder encounter groups — HTML5 DnD from grip in turn column; drop on group wrapper reorders `encounterGroups` + `groupTurnActed`; captain refs + EoT maps + SE elapsed set remapped via `remapEncounterGroupIndex`; pending EoT timers cleared + rescheduled on new indices (30s window restarts — acceptable edge)

**Key decisions:**
- `EncounterGroup.id` (uuid) for stable React keys; `newEncounterGroupId()` on clone + add group
- Pure helpers in data.ts: `moveIndexInArray`, `remapEncounterGroupIndex`, `reorderEncounterGroupsWithCaptainRemap`, `ENCOUNTER_GROUP_DRAG_MIME`
- `scheduleEotTimerForGroup` extracted from useEffect; reorder clears timer map then `queueMicrotask` reschedules for remapped keys; `prevTurnActedRef` init from ENCOUNTER_GROUPS length + manual remap on reorder keeps turn transition effect sane
- Drop target wrapper in App (`data-testid=encounter-group-drop-target`); ring highlight on drag-over
- GroupTurnColumn optional `encounterGroupDragHandle` (6-dot svg, `aria-label` reorder)

**Files changed:**
- src/types.ts
- src/data.ts
- src/data.test.ts
- src/App.tsx
- src/App.test.tsx
- src/components/TurnColumnCell.tsx
- src/components/GroupSection.tsx
- src/components/GroupSection.test.tsx
- src/components/StatBlock.test.tsx
- PRD.json


[DND-002]

Monster row DnD — 6-dot grip left of ordinal on each monster/minion parent row; drop target = name cell (`monster-drop-target` + data-group-index/monster-index); insert-before `toMonster`; empty group gets `empty-group-monster-drop-target` row

**Key decisions:**
- `MONSTER_DRAG_MIME` JSON `{fromGroup,fromMonster}` — orthogonal to `ENCOUNTER_GROUP_DRAG_MIME` so group wrapper dragOver/drop ignores monster drags
- `moveMonsterInEncounterWithCaptainRemap` in data.ts: splice out/in then remap every `captainId` via remove-then-insert index rules (refs to moved monster → new slot; same-group shifts; dest group bump ≥ insert)
- `remapEotConfirmedAfterMonsterMove` for cross-group + same-group EoT key strings (`mi:label` / `mi:mni:label`)
- EoT map update in `queueMicrotask` after `setEncounterGroups` schedules (avoids nested setState in updater)
- 7 data unit tests + 4 App tests (handles, intra-group reorder, cross-group, empty group)

**Files changed:**
- src/data.ts
- src/data.test.ts
- src/App.tsx
- src/App.test.tsx
- src/components/GroupSection.tsx
- src/components/MonsterRowCells.tsx
- src/components/MinionGroupRow.tsx
- PRD.json


[DND-003]

Condition DnD — active condition icon buttons draggable; drop on another creature condition cell transfers label+state; orthogonal MIME `CONDITION_DRAG_MIME` vs monster/group drags

**Key decisions:**
- `transferConditionBetweenCreatures` + `parseConditionDragPayload` in data.ts; same-monster parent↔minion path vs cross-creature path
- Active strip buttons only `draggable`; `CreatureConditionCell` drop zone + emerald ring highlight; `data-testid=condition-drop-target` + indices for tests
- EoT confirm key cleared on successful transfer from source (queueMicrotask) to avoid stale confirmation
- Minion parent + child rows wired via `bindConditionDnD` in GroupSection

**Files changed:**
- src/data.ts
- src/data.test.ts
- src/App.tsx
- src/App.test.tsx
- src/components/ConditionCatalogIconStrip.tsx
- src/components/ConditionCatalogIconStrip.test.tsx
- src/components/CreatureConditionCell.tsx
- src/components/GroupSection.tsx
- src/components/MonsterRowCells.tsx
- src/components/MinionGroupRow.tsx
- PRD.json


[A11Y-002]

Keyboard for all dropdowns — shared `dropdownA11y.ts` (listFocusableIn, tabWrapKeyDown, focusRelativeIn); focus-in + focus restore on open/close; Tab wrap; Arrow/Home/End; Escape (doc listener on AddMonster when open so Escape works if input not focused)

**Key decisions:**
- Condition picker + captain listbox + color dialog: initial focus first control, restore prior/anchor on close
- AddMonster: trigger ref for restore (button unmounts when open); `onKeyDownCapture` so ArrowDown leaves search before input eats it; `data-bestiary-option` for list-only arrow moves; doc Escape matches existing outside-click pattern
- Color popover portal: key listener on menu div after rAF; restore `anchor` when `!open`
- Unit tests in `dropdownA11y.test.ts`; AddMonster + GroupColorPicker integration tests for ArrowDown/Up + focus + ArrowDown row

**Blockers/notes:** stamina hover editor still hover/focus-within only (not list dropdown). A11Y-001 still open.

**Files changed:**
- src/dropdownA11y.ts (new)
- src/dropdownA11y.test.ts (new)
- src/components/CreatureConditionCell.tsx
- src/components/AddMonsterButton.tsx
- src/components/AddMonsterButton.test.tsx
- src/components/MinionGroupRow.tsx
- src/components/GroupColorPickerPopover.tsx
- src/components/GroupColorPickerPopover.test.tsx
- PRD.json


[STAT-002]

Draw Steel stat block styling + official DrawSteelGlyphs-Regular.otf — amber/zinc book-like panels, MARIP letters+nums + stamina/speed/size/stability/FS in glyph font, power-roll rows prefixed !/@/#, mapped keywords as icon chips (Melee→t etc. from MCDM chart 002.101 via pdftotext). Unmapped keywords stay text pills. Font+CC-BY-SA license copied to public/fonts/.

**Key decisions:**
- `drawSteelGlyphs.ts` keyword map from chart PDF; tier glyphs ! @ #; no automatic damage-type diamond substitution (chart order unclear)
- `@font-face` + `.font-draw-steel` in index.css; tests assert structure/ARIA not font raster
- Core header: split speed digit (glyph) vs movement parenthetical (sans)

**Blockers/notes:** A11Y-001 still open. Extend keyword map as needed from full glyph chart.

**Files changed:**
- public/fonts/DrawSteelGlyphs-Regular.otf
- public/fonts/DrawSteelGlyphs-LICENSE.txt
- src/drawSteelGlyphs.ts (new)
- src/drawSteelGlyphs.test.ts (new)
- src/index.css
- src/components/StatBlock.tsx
- src/components/StatBlock.test.tsx
- PRD.json


[ADV-001]

Persist encounter state to localStorage — auto-saves encounterGroups, terrainRows, groupTurnActed on every state change; loads on init with fallback to defaults

**Key decisions:**
- persistence.ts module: serialize/deserialize with version field for future migration; features stripped on save (re-derived from bestiary on load) to keep payload lean
- initStateFromStorage() in App.tsx: single useState init reads localStorage once; avoids double-render or useEffect-based loading
- saveToLocalStorage catches quota errors gracefully (returns {ok:false}); loadFromLocalStorage catches SecurityError/corrupt JSON
- Ephemeral state (EoT timers, SE act window, drawer, drag) NOT persisted — session-only
- prevTurnActedRef init uses loaded turn state instead of hardcoded ENCOUNTER_GROUPS.map(()=>false)
- Node 25 built-in localStorage shadows jsdom in vitest; used vi.stubGlobal mock in integration tests
- 21 persistence unit tests + 6 App integration tests

**Files changed:**
- src/persistence.ts (new)
- src/persistence.test.ts (new)
- src/App.tsx
- src/App.test.tsx
- PRD.json


[ADV-002]

Reload localStorage state on page load — implementation already existed from ADV-001 (initStateFromStorage reads localStorage once on init, falls back to defaults); added 6 ADV-002-specific integration tests + fixed 4 pre-existing lint errors

**Key decisions:**
- No new runtime code needed; ADV-001's initStateFromStorage() already covers all ADV-002 requirements (check → deserialize → fallback)
- 6 new App integration tests: turn-acted restore, condition restore, empty localStorage fallback, version mismatch fallback, SecurityError fallback, deleted-group state restore
- Fixed 4 pre-existing lint errors: removed unused `remapEotConfirmedAfterMinionReorder` import, prefixed unused destructured vars with `_`, added `@typescript-eslint/no-unused-vars` config for `_`-prefixed vars

**Files changed:**
- src/App.test.tsx
- src/App.tsx
- src/components/MinionGroupRow.tsx
- eslint.config.js
- PRD.json


[ADV-003]

Create new encounters and name them — multi-encounter persistence model, "New" button in header opens name prompt, creates encounter with default seed data under per-encounter localStorage key

**Key decisions:**
- Encounter index (`live-steel-encounter-index`) stores `{encounters: [{id, name}], activeId}` separate from per-encounter data (`live-steel-encounter-{id}`)
- Legacy single-encounter key auto-migrated via `migrateFromLegacyStorage()` on first load — existing users see "Encounter 1" with preserved data
- `initStateFromStorage()` expanded to return encounter metadata alongside state; App tracks `encounterIndex`, `activeEncounterId`, `encounterName` in state
- `saveToLocalStorage`/`loadFromLocalStorage` gain optional `encounterId` param — backward-compatible for legacy tests
- "New" button inline next to encounter name; prompt dialog with text input, Create/Cancel buttons, Enter/Escape keys
- `createNewEncounter()` saves current encounter state before switching to fresh default data; clears EoT timers + confirmed sets
- Encounter name replaces the old static "Encounter roster" subtitle
- 12 new persistence unit tests (index CRUD, migration, deletion, id isolation) + 11 new App integration tests (name display, prompt UI, creation flows, persistence, legacy migration)

**Files changed:**
- src/persistence.ts
- src/persistence.test.ts
- src/App.tsx
- src/App.test.tsx
- PRD.json


[ADV-004]

Switch between encounters — encounter name becomes clickable dropdown listing all encounters; selecting one saves current state and loads target; ephemeral state (EoT timers, confirmed sets, drawer) reset on switch

**Key decisions:**
- Encounter name in header replaced with button + chevron; click toggles a listbox dropdown of all encounters from encounterIndex
- switchToEncounter() saves current encounter to localStorage before loading target; clears EoT timers, confirmed sets, SE act window, and monster card drawer
- Falls back to default seed data if target encounter's localStorage data is missing/corrupt
- Active encounter highlighted with amber text + checkmark icon; aria-selected for accessibility
- Dropdown closes on: option select, Escape key, outside click (mousedown listener)
- No-op when clicking the already-active encounter
- 13 new App integration tests: button presence, dropdown open, encounter listing, aria-selected, switching + state restore, save-before-switch round-trip, dropdown close, Escape, outside click, index activeId update, condition preservation, reload persistence, no-op on active click

**Files changed:**
- src/App.tsx
- src/App.test.tsx
- PRD.json
