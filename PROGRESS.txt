[STAT-001]

Expandable/collapsible stat blocks per monster and minion row

**Key decisions:**
- Added MonsterFeature/PowerRollEffect types to types.ts; Monster gets optional features field
- StatBlock component splits features into Abilities and Traits sections w/ power roll tier tables
- Expand/collapse toggle button appears only when monster has features; chevron icon in condition cell column
- Stat block row spans gridColumn 2/-1 below monster row; GroupSection tracks expandedStatBlocks state and adjusts grid row counts
- MinionGroupRow gets separate stat block toggle alongside existing minion expand chevron
- Seeded features data inline in ENCOUNTER_GROUPS for Goblin Assassin, Underboss, and Minions (Spinecleaver)
- 22 new tests covering: StatBlock rendering, MonsterRowCells toggle, GroupSection integration, minion group stat block

**Files changed:**
- src/types.ts
- src/data.ts
- src/components/StatBlock.tsx (new)
- src/components/StatBlock.test.tsx (new)
- src/components/MonsterRowCells.tsx
- src/components/MinionGroupRow.tsx
- src/components/GroupSection.tsx
- PRD.json


[STAT-003]

Source stat block data from SteelCompendium bestiary — bestiary.ts loads data/bestiary/Monsters/statblocks.json (416 monsters), builds name-keyed lookup map, maps raw JSON → MonsterFeature[]

**Key decisions:**
- Direct JSON import of statblocks.json via Vite's resolveJsonModule; no runtime fetch needed
- baseName() strips trailing ordinal ("Goblin Assassin 1" → "Goblin Assassin") so numbered encounter copies resolve to the base statblock
- resolveFeatures() in data.ts tries monster name first, then first minion's name (handles generic parent names like "Minions" for minion groups), then falls back to inline seed data
- Removed hardcoded features from ENCOUNTER_GROUPS seed data; bestiary is now source of truth
- mapFeatures() filters to ability/trait only (excludes subtrait), omits optional fields when absent
- 30 new tests covering: baseName, statblockCount, statblockNames, lookupStatblock, mapFeatures, featuresForMonster, cloneEncounterGroups bestiary integration

**Files changed:**
- src/bestiary.ts (new)
- src/bestiary.test.ts (new)
- src/data.ts
- PRD.json
